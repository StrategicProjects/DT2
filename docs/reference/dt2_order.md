# Define initial ordering (option `order`)

Define initial ordering (option `order`)

## Usage

``` r
dt2_order(options = list(), ...)
```

## Arguments

- options:

  Options list.

- ...:

  Vectors like `c(col, "asc"/"desc")`. `col` may be name or 1-based
  index.

## Value

Updated `options`.
