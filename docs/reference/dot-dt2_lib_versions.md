# Get all current library versions in DT2

Core library versions here must be kept in sync with `R/dt2_deps.R` and
`tools/get-dt2-libs.sh`. Extension versions are read from the registry
automatically.

## Usage

``` r
.dt2_lib_versions()
```

## Value

Named character vector
