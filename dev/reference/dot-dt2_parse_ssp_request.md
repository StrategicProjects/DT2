# Internal: parse DataTables server-side request (query string to list)

Internal: parse DataTables server-side request (query string to list)

## Usage

``` r
.dt2_parse_ssp_request(req, n_cols)
```
