# Auto-detect extensions from options

Scans the options list for keys that imply certain extensions. For
example, `buttons` implies Buttons, `select` implies Select, etc.

## Usage

``` r
.dt2_detect_extensions(options)
```

## Arguments

- options:

  DataTables options list

## Value

character vector of detected extension names
